# Embeddings

This folder is reserved for future vector-ready outputs, chunk manifests or references to an external vector database.

Do not commit large generated embedding files without a clear storage plan.

Possible future storage targets:

- Local JSONL chunk manifests.
- SQLite or DuckDB prototypes.
- External vector database references.
- n8n / LangChain pipeline outputs.
